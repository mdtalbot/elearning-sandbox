<%= form_with(model: lesson_oral_argument, local: true) do |form| %>
  <% if lesson_oral_argument.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(lesson_oral_argument.errors.count, "error") %> prohibited this lesson_oral_argument from being saved:</h2>

      <ul>
      <% lesson_oral_argument.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
