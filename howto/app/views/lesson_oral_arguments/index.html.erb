<p id="notice"><%= notice %></p>

<h1>Lesson Oral Arguments</h1>

<table>
  <thead>
    <tr>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @lesson_oral_arguments.each do |lesson_oral_argument| %>
      <tr>
        <td><%= link_to 'Show', lesson_oral_argument %></td>
        <td><%= link_to 'Edit', edit_lesson_oral_argument_path(lesson_oral_argument) %></td>
        <td><%= link_to 'Destroy', lesson_oral_argument, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Lesson Oral Argument', new_lesson_oral_argument_path %>
