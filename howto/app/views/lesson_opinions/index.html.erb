<p id="notice"><%= notice %></p>

<h1>Lesson Opinions</h1>

<table>
  <thead>
    <tr>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @lesson_opinions.each do |lesson_opinion| %>
      <tr>
        <td><%= link_to 'Show', lesson_opinion %></td>
        <td><%= link_to 'Edit', edit_lesson_opinion_path(lesson_opinion) %></td>
        <td><%= link_to 'Destroy', lesson_opinion, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Lesson Opinion', new_lesson_opinion_path %>
