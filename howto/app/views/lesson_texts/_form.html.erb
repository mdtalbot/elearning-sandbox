<%= form_with(model: lesson_text, local: true) do |form| %>
  <% if lesson_text.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(lesson_text.errors.count, "error") %> prohibited this lesson_text from being saved:</h2>

      <ul>
      <% lesson_text.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :adv %>
    <%= form.text_field :adv %>
  </div>

  <div class="field">
    <%= form.label :int %>
    <%= form.text_field :int %>
  </div>

  <div class="field">
    <%= form.label :sim %>
    <%= form.text_field :sim %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
